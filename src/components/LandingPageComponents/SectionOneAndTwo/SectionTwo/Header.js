import React from "react";
import styled from "styled-components";

const Header = styled.p`
  font-family: Campton-Bold;
  font-size: 20px;
  color: #202020;
`;

const SectionTwoHeader = () => <Header>What you can win inside</Header>;

export default SectionTwoHeader;
